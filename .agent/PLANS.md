# ExecPlan doctrine

The full doctrine — requirements, formatting, milestones, living-document rules, the skeleton — lives in the `crafted` plugin's `exec-plan` skill. Read `${CLAUDE_PLUGIN_ROOT}/skills/exec-plan/references/PLANS.md` in full before authoring or revising a plan, and follow it to the letter; the plugin's copy is canonical. Store plans themselves in `.agent/exec-plans/`, one per feature.
